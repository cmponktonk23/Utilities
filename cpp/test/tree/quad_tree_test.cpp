// #include "tree/quad_tree.h"
#include <gtest/gtest.h>
// #include "common/types/rect.h"
// #include "tree/inside_handler.h"

namespace utilities {
TEST(QuadTreeTests, BuildTest1) {
//   auto handler = RectInsideRectHandler();
//   QuadTree<Rect, RectInsideRectHandler> tree(handler);
//   ASSERT_TRUE(tree.IsEmpty());

//   Rect area(Vector2D(0, 0), Vector2D(100, 100));
//   std::vector<Rect> objects;
//   objects.push_back(Rect(Vector2D(0, 0), Vector2D(5, 5)));
//   objects.push_back(Rect(Vector2D(10, 10), Vector2D(5, 5)));
//   //   auto objects = std::vector<Rect>{Rect(Vector2D(0, 0), Vector2D(5, 5)), Rect(Vector2D(10, 10), Vector2D(5, 5))};

//   tree.Build(area, objects);
//   ASSERT_FALSE(tree.IsEmpty());
}
}  // namespace utilities